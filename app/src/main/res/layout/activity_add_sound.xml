<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#B280FF"
    android:padding="24dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="24dp">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_revert"
            android:tint="#FFFFFF" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Add Sound to Playlist"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginStart="16dp"
            android:gravity="center_vertical" />
    </LinearLayout>

    <!-- Info Text -->
    <TextView
        android:id="@+id/tvInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You can add sound files from local storage, assets, or URL"
        android:textSize="14sp"
        android:textColor="#FFFFFF"
        android:background="@drawable/bg_cloud"
        android:padding="16dp"
        android:layout_marginBottom="24dp" />

    <!-- Sound Name Input -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sound Name *"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/etSoundName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="e.g., Relaxing Rain"
        android:background="@drawable/bg_edittext"
        android:padding="16dp"
        android:textSize="16sp"
        android:layout_marginBottom="24dp" />

    <!-- Sound Path Input -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sound Path / URL *"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/etSoundPath"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="file:///path/to/sound.mp3 or asset://sound.mp3 or http://..."
        android:background="@drawable/bg_edittext"
        android:padding="16dp"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <!-- Choose File Button -->
    <Button
        android:id="@+id/btnChooseFile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📁 Choose File from Device"
        android:textSize="16sp"
        android:background="@drawable/btn_rounded"
        android:textColor="#FFFFFF"
        android:padding="16dp"
        android:layout_marginBottom="24dp" />

    <!-- Save Button -->
    <Button
        android:id="@+id/btnSave"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💾 Save to Playlist"
        android:textSize="18sp"
        android:textStyle="bold"
        android:background="@drawable/btn_rounded"
        android:textColor="#FFFFFF"
        android:padding="20dp" />

</LinearLayout>

